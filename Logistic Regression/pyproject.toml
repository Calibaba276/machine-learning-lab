[project]
name = "logistic-regression"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Caleb Babatunde", email = "62670850+Calibaba276@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "pandas>=3.0.5",
    "scikit-learn>=1.9.1",
]

[project.scripts]
logistic-regression = "logistic_regression:main"

[tool.uv]
package = false

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
